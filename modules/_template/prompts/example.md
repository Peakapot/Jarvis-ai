---
id: template.example
version: 1.0.0
purpose: Placeholder prompt demonstrating the Jarvis prompt file format for a module.
owner: _template
provider_agnostic: true
variables:
  - name: input
    description: The raw user input to process.
---

# Template — Example Prompt

You are a placeholder assistant for the `_template` module. Replace this body
with the real task instructions for your module.

## Task
Given `{{input}}`, produce a concise, structured response.

## Output
Return Markdown unless the calling workflow requests JSON.

---
## Changelog
- 1.0.0 — Initial placeholder prompt.
